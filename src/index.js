import * as React from "react";
import * as ReactDOM from "react-dom";
import * as MUI from "@material-ui/core";

ReactDOM.render(
  <MUI.Box component="h1" mr={3} color="teal">
    This should be teal
  </MUI.Box>,
  document.getElementById("root")
);
